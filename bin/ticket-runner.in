#!/usr/bin/env bash
# Generated by install.sh — runs ticket-runner from its install directory.
set -euo pipefail

APP_DIR="@APP_DIR@"
export PYTHONPATH="$APP_DIR/src${PYTHONPATH:+:$PYTHONPATH}"
export PYTHONUNBUFFERED=1

exec "@PYTHON@" -m ticket_runner "$@"
